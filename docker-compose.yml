services:
  app:
    build: .
    ports:
      - "8080:8080"
    env_file: .env
    dns: 8.8.8.8
    cap_add:
      - NET_ADMIN
      - NET_RAW
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 15s

  smscsim:
    image: ukarim/smscsim:latest
    ports:
      - "2776:2775"
      - "12775:12775"
    restart: unless-stopped
